[package]
name = "oauth2aas"
version = "1.0.0"
edition = "2024"

[dependencies]
gpapi = { path = "../gpapi" }
tokio = { version = "1.39.2", features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
