name = "sniff"
main = "build/worker/shim.mjs"
compatibility_date = "2025-04-25"

routes = [
  { pattern = "example.com", custom_domain = true }
]

[build]
command = "cargo install -q --locked worker-build@0.8.6 && worker-build --release"

[vars]
DEVICE_NAME="sm_a13_5g"
